# Personal access token used by .mcp.json to authenticate the GitHub MCP server.
# Create one at: https://github.com/settings/personal-access-tokens
# Scopes needed: repo (read/write), and "workflow" if you want it triggering Actions.
# Fine-grained tokens scoped to just this repo are recommended over classic tokens.
GITHUB_PAT=
