PORT=5000

# Either set a full connection string...
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/expense_tracker

# ...or individual fields (used if DATABASE_URL is not set)
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=expense_tracker
